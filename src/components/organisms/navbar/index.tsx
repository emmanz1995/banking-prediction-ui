import './navbar.scss';

const Navbar = () => {
  return (
    <div className="navbar">
      <div className="title">
        <h3>Hello World!</h3>
        {/* <p>{currentUser?.username === undefined ? 'you are log out ' : 'Actively logged in'}</p> */}
      </div>
    </div>
  );
};

export default Navbar;
